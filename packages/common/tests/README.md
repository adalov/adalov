# @adalov/common Unit Tests

Unit tests for `@adalov/common` use the repository-native test stack documented in [`docs/development.md`](../../../docs/development.md#unit-testing).

Test files use the `*.test.ts` suffix and should prefer the package public API when practical.
